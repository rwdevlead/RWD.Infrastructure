---
portainer_agent_dir: "{{ docker_base_dir }}/portainer-agent"
portainer_agent_port: 9001
portainer_agent_secret: "{{ lookup('env', 'PORTAINER_AGENT_SECRET') }}"
portainer_agent_watchtower_enable: true
