services:
  agent:
    image: portainer/agent:latest
    container_name: {{ portainer_agents[inventory_hostname].name }}
    restart: always
    labels:
      - "com.centurylinklabs.watchtower.enable={{ portainer_agent_watchtower_enable }}"
    ports:
      - "{{ portainer_agent_port }}:9001"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /var/lib/docker/volumes:/var/lib/docker/volumes
    networks:
      - {{ shared_docker_network }}

networks:
  {{ shared_docker_network }}:
    external: true