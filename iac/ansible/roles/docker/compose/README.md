# docker/compose

Installs Docker Compose v2 as a Docker plugin.

## Responsibilities

- Install docker-compose-plugin package
- Ensure `docker compose` is available

## Notes

- Uses Docker’s native plugin (v2)
- Preferred over legacy docker-compose
- Requires docker/engine role
