# Terraform
# Local .terraform directories
.terraform/

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Packer
# Cache objects
packer_cache/

# Packer variable files (may contain sensitive data)
*.pkrvars.hcl
*.pkrvars.json

# For `packer init` downloaded plugins
.packer.d/plugins/

# Build artifacts
builds/
output-*/

# Crash logs
packer-crash.log

# Ansible
# Galaxy-installed roles and collections
roles/*
!roles/requirements.yml
collections/*
!collections/requirements.yml

# Ansible Vault password file
.vault_pass

# Inventory files that may contain sensitive data
inventory/*
!inventory/example.ini
!inventory/group_vars/example.yml

# Generated retry files
*.retry

# Ansible fact cache
.ansible/fact-cache/

# Local configuration overrides
ansible.cfg

# Don't ignore these if they exist
!roles/*/

# Environment files
.env
.env.*
!.env.example

# VS Code settings
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# OS specific files
.DS_Store
Thumbs.db
